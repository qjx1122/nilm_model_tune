# TUNING_GUIDE.md — Transformer-NILM 调参人话指南（面向无算法背景工程师）

> 定位（依据 BOOTSTRAP.md v2.1 台账）：调参教学文档——人话版方法论与战史。**方法论 / SOP 变化时整篇重写对账，不追加流水**；每次实验的原始记录进 `REPORT_TEST.md`，这里只留"能长期用的人话规则"。
> v1.0（2026-09-08）：随「Transformer-NILM 调参方案设计」专题建立。正式方案全文见 `REPORT_TEST.md` 同名专题节。

---

## 0. 三句话先记住调参是干什么
1. 模型是一台"照着总电表功率猜烧水壶功率"的机器，旋钮（超参数）决定它学多快、记得多牢、还是学傻。
2. 调参 = 有纪律地试错：**每次只回答一个问题，改一个方向，拿数字说话**。
3. 纪律比技巧重要。最毁实验的不是参数差，而是"用考试卷调答案"（见 §1）。

## 1. 为什么必须守住 Validation / Test 界线（考试卷比喻）
- Test 集 = 期末考卷：一生只能考一次，考完这学期就结束了。
- Validation 集 = 每周模拟考：随便考，用来决定还要不要改学习方法。
- 拿 Test 调参 = 对着期末答案改复习计划 → 分数当然高，但那是**背答案**，不是学会。
- 本项目的红线：`tune.py` 原本按 test 排名，已列为必须改造项（REPORT_TEST.md 代码改造 #1）。看到任何"用 test 选的配置"，第一反应是：不作数。

## 2. 通用流程五步（本项目 SOP 的骨架）
```text
准备数据 → 跑基线(×3个随机种子) → 粗搜(随机30个左右) → 细搜(前3名邻域×3种子) → 锁定配置 + Test只考一次
```
每步只有在上一步数字正常时才往前走；哪步不对就停在哪步修，不要硬冲。

## 3. 术语白话表
| 术语 | 人话 | 拧错会怎样 |
| --- | --- | --- |
| lr（学习率） | 每步学多"大" | 太大：原地抽风（loss 震荡）；太小：学得比树懒慢 |
| batch_size | 一次看多少条样本再更新 | 太小：步子碎、抖；太大：显存爆、收敛钝 |
| epochs | 整份题刷几遍 | 太少没学会；配合早停，多设无害 |
| patience（早停） | 连续几轮模拟考没进步就停 | 太小：差一点点火候就停；太大：浪费时间 |
| dropout | 每轮随机让一部分神经元"请假"，防死记硬背 | 太大：学不进去（欠拟合）；太小：背题（过拟合） |
| weight_decay | 给权重"瘦身税"，防权重过大 | 太大：模型被压得学不动 |
| d_model / num_layers / dim_feedforward | 模型的"脑容量 / 层数 / 中间思考宽度" | 太大：背题过拟合 + 慢；太小：装不下规律 |
| window_size | 猜当前功率时往前看多长一段总负荷 | 太长：包含无关历史；太短：没看到烧水开始的证据 |
| seed（随机种子） | 给"运气"上编号，同号同结果 | 不复现 = 无法判断是参数好还是运气好 |
| ON/OFF 阈值 | 预测功率超过多少 W 算"壶开着" | 本任务固定 500W（kettle 工作约 2–3kW） |
| 综合分 S | 把 MAE、F1、能量误差合成一个排名分 | 见 §6 |

## 4. 本项目每个旋钮是干什么的（kettle 场景直觉）
- **window_size**：水壶烧水 1–5 分钟，6 秒一条数据 → 128 窗 ≈ 看 12.8 分钟，够看到"开→关"全过程；64 偏短、256 偏长（看太多无关背景）。
- **lr**：本模型很小（几十万参数），AdamW 下 3e-4~1e-3 是舒适区；从 5e-4 起步。
- **dropout / weight_decay**：Train 好 Val 差（背题）时优先加；Train Val 都差时先别碰它俩，先查数据和学习率。
- **d_model 与 nhead**：必须整除（32 配 4/8，64 配 4/8，128 配 8）；不是越大越好，kettle 这种单电器任务 d_model=64 往往够。
- **epochs/patience**：25 轮上限 + 连续 5 轮没进步就停，足够；跑得飞快说明早停生效，不是 bug。

## 5. 判断分支速查表（现象 → 先查什么）
| 现象（看 Train / Val 曲线） | 含义 | 先做（按序） |
| --- | --- | --- |
| Train 差 + Val 差 | 没学会 | ①数据对齐/归一化 ②lr ③window ④模型容量 |
| Train 好 + Val 差 | 背题（过拟合） | ①dropout ②weight_decay ③减层数 ④（收尾再想数据增强） |
| Train/Val 好 + Test 差 | 换了考场就不会 | ①时间分布漂移 ②预处理不一致 ③切分是否跨家庭 |
| loss 震荡 | 步子太大 | 先降 lr（×0.3~0.5），看是否平复 |
| loss 降得极慢 | 步子太小/结构问题 | 提 lr（×2~3）但盯 Val；仍慢再查数据 |
| MAE 好但 RMSE 差 | 大部分准，少数大错 | 查事件启动/关闭段与高峰值区间的预测 |
| F1 好但能量误差大 | 开关时刻对，功率幅度偏 | 看 ON 段平均预测 vs 真实（系统性偏低/偏高） |
| 两次同配置结果差很多 | 运气/随机性 | 多 seed 复跑报 mean±std，别拿单次下结论 |
| 排行榜第一名总在变 | 试太多+看太多 | 回到粗搜规模，看 top-3 而不是第一名 |

## 6. 综合分 S 怎么读（本项目业务口径）
- 业务问题只有两个：**"壶现在开没开对？"（F1）** 和 **"一天烧水用了多少电？"（能量误差）**，然后才是曲线贴不贴（MAE）。
- 所以排名用 `S = 0.4·(MAE/2000W) + 0.4·(1−F1) + 0.2·|能量误差|`，越小越好；先过硬门槛（F1≥0.75、recall≥0.70、|能量误差|≤0.15）再谈排名。
- 只信 Validation 上的 S；Test 的 S 只许出现两次（基线一次、最终一次）。

## 7. 本项目当前 SOP 摘要（2026-09-08 生效）
- 数据：House1、6s、aggregate→kettle，npz 制备规格与验收见 REPORT_TEST.md 阶段 0。
- 搜索空间与预算：随机 32 trials（单 trial 实测 >60s 减半）；细搜 top-3 邻域 ×3 seeds；阶段 0 先跑 baseline ×3 seeds 校准门槛并回填预算。
- 代码前置改造 4 项（按序）：tune.py 改按 val 排名 →（可选）fit 判据参数化 → prepare_ukdale.py 新建 → tuning.yaml 对齐。改造前现有 tune.py 输出**不作数**。

## 8. 战史（执行实录与经验教训）
- （空）等待真实 UK-DALE 实验。执行记录按 REPORT_TEST.md 专题节追加，方法论沉淀到本节重写对账。

## 🗒 修订记录
- v1.0（2026-09-08）：初始版本——随调参方案专题建立；内容 = 守则（test 红线）/ 术语表 / 判断分支 / 综合分口径 / SOP 摘要。
