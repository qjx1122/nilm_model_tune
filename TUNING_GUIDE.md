# TUNING_GUIDE.md — Transformer-NILM 调参人话指南（面向无算法背景工程师）

> 定位（依据 BOOTSTRAP.md v2.1 台账）：调参教学文档——人话版方法论与战史。**方法论 / SOP 变化时整篇重写对账，不追加流水**；每次实验的原始记录进 `REPORT_TEST.md`（执行实录 1–20），稳定结论进 `REPORT.md`。
> v2.0（2026-09-09）：任务 3「调参执行」收官，整篇重写——v1.0 只有开战前的方法论，v2.0 补上完整战史（两个数据纪元、五层 bug、最终锁定与验收）。

---

## 0. 三句话先记住调参是干什么

1. 模型学的是"从全家总用电曲线里认出水壶开机的那一段"——**它学什么，完全取决于你喂什么数据**。
2. 调参是在"考试规则"（KPI）固定的前提下，找一组让模型考得最好的设置（窗口、层数、学习率……）。
3. 最大的坑从来不是"参数没调好"，而是**数据有问题、指标会骗人、或者你偷看了考卷**。

---

## 1. 六条铁律（战火里验证过的方法论）

### 铁律 1：先证明数据是对的，再调参
本项目最大的一次收益来自修数据，不是调参：数据修好、一个参数没动，Test 能量误差从 −23.4% 直接缩到 −6~−9%——超过此前全部调参努力之和。**数据身份验证三件套**（diagnose_split.py）：
- `aggOffW`（壶关着时总负荷均值）≈0 → aggregate 里没有别的电器，NILM 前提失效；
- `corr`（壶 vs 总负荷相关）≈1 → 总负荷里几乎只有壶，同上红旗；
- `evt/day` + 壶功率量级 → 对照常识（水壶一天烧 4–6 次、2300W 级）。

### 铁律 2：Test 是预算，不是仪表盘
考卷（Test）看一次，对它的敬畏就少一分。规矩：**每个数据纪元只许碰 2 次**（摸底一次、最终锁定一次），选型全程只看 Validation。本项目把 `eval_test` 代码缺省值改成 False——不显式 `--test` 就碰不到，纪律焊进代码里。

### 铁律 3：单 seed 的排名是彩票
32 个配置各跑一个 seed，第一名有 1/32 的概率纯粹是走运——实测两次：粗搜 top-1 单 seed 0.0400，三 seed 复核跌回 0.0567（"赢家诅咒"）。**规矩：候选配置必须 ≥3 个 seed 配对复核（同 seed 族），报告 mean±std；没有锚（不调参基线）的排名分不清"发现"和"运气"。**

### 铁律 4：先看 val 的分辨率，再信 val 的数字
F1 的最小刻度 = 1/(壶开着的样本数)。6000 样本的 val 里壶只开 33 个样本 → F1 三个种子一字不差（不是稳，是刻度太粗）；综合分 S 的方差全部来自能量误差——恰好是最噪的指标。**规矩：调参前先算 val 的 ON 样本数，本项目的答案是 30000（180 ON）。**

### 铁律 5：一次只动一个变量
想知道"窗口 96 是不是更好"，就把冠军配置原封不动只改窗口（F4 实验）；想知道"旧配置还灵不灵"，就原封不动平移过来（探针 A）。多变量同时动，赢了也不知道该谢谁。

### 铁律 6：判定规则在看数据之前定好（预注册）
锁定条件、Test 验收线、判定树，全部在跑实验**之前**写进台账。事后挑规则 = 变相偷看考卷。本项目 F4 的锁定就是预注册判定树机械执行的结果，没有人为裁量空间。

---

## 2. SOP 流程（下次照着走）

```
① 数据制备（prepare）：留痕每一步（data_spec.json）
② 身份验证（diagnose）：aggOffW / corr / evt/day 三指标 + 常识对照
③ Test 摸底（触碰 #1）：baseline ×3 seeds，建立"数据纪元锚"
④ 两个探针：旧优胜平移（测可迁移性）+ 单变量杠杆（测数据量）
⑤ 随机粗搜（tune.py）：val 够大，gates 守门，只看 val 排名
⑥ 细搜：top-3 + 双锚（旧冠军、不调参基线）×3 seeds 配对
⑦ 锁定（预注册判定树）→ Test 终局一跑（触碰 #2，预算耗尽）
⑧ 收官：REPORT.md 稳定结论 + 本指南战史 + STATUS 收尾仪式
```

---

## 3. 战史（2026-09-08 ~ 09-09，两个数据纪元）

### 第一纪元：在坏数据上调参（白忙，但不是白忙）

粗搜 32 trials → 细搜 7 变体 ×5 seeds → 锁定 v2_do00（w128 d64 nhead8 do0 bs64 lr3e-4）。过程中学会了赢家诅咒、种子族效应、配对复核——这些纪律全部是真金白银买的。但最终 Test 能量误差 −23.4%，远超 15% 验收线：**模型被要求从一条"总负荷里混着锅炉、还把锅炉算了两遍"的曲线里认水壶**。判读：不是模型不行，是数据在撒谎。方向 A：数据法证先行。

### 洋葱行动：五层 bug，一层一层剥

| # | 症状 | 真相 | 修法 |
| --- | --- | --- | --- |
| 1 | EE −23% | aggregate=meter1+meter2，而 meter2 是锅炉回路——**双计** | metadata 全文定表号：mains 只用 meter1 |
| 2 | prepare n=345 | 各表采样秒针错开（:15 vs :18），精确对齐拼不上 | 先 resample 到统一 6s 网格 |
| 3 | n=2058 | 选段索引 bug + 多表求和把缺口静默写成 **0W 假零** | 修 bug + `min_count=1` |
| 4 | 240 万缺口只补了 49 格 | `fillna(0, limit=N)` 的 limit 是**全轴总限额**，不是每段限额（pandas 语义陷阱） | run-length 整段桥接 |
| 5 | 事件率翻倍（12.6 次/天） | 煮沸中途掉线被补 0，一次煮沸切成 3 片 35 秒碎片（ON 样本总数不变是铁证） | kettle 缺口改 ffill 前值（煮沸中保持 2300W） |

剥完五层：n=10,377,650（720.7 天），456 处接缝，最大连续段 49.1 天，红旗三验通过，**数据锁定（schema v5）**。旧 npz 上的一切 KPI 作废归档，Test 预算重置。

### 第二纪元：v5 重启 → F4 锁定 → Test 通过

1. **摸底**（触碰 #1）：baseline ×3 seeds，EE −6~−9%，全门槛过——数据修复红利兑现。
2. **双探针**：A=旧冠军 v2_do00 平移，配对全胜（S 0.033 vs 0.048、EE 归零）→ 旧洞察可迁移；B=训练量 30k→100k，打平且 2.7 倍代价 → 弃。
3. **重搜**：32/32 过门槛；**w96 崛起**（top-10 占 7 席）；val 扩 30000 后 F1 恢复分布。
4. **细搜批次 1**：F0（旧冠军）0.0522 夺冠、FB（不调参）垫底——调参价值坐实；但搜索 top-1 赢家诅咒回落。
5. **批次 2（单变体）**：F4=冠军协议×w96 → **0.0472，五项第一，MAE 盆地治愈**，预注册判定树命中，锁定。
6. **Test 终局**（触碰 #2，seed 7000）：**S 0.0541 / F1 0.8941 / R 0.9268 / EE +5.2%——四线全过，验收通过。**

最终冠军是**两个纪元的杂交**：旧纪元练出的协议（do0/nhead8/bs64/lr3e-4）× 新纪元搜索发现的窗口（w96）。缺了哪一半都到不了这个配置。


### 第三纪元（2026-09-10）：House2 kettle → f5_t9 锁定 → Test 通过

换房子换壶就是换世界：同一套代码、同一套流程，House2 从身份验证到 Test 收官一天跑完——但**每一个架构结论都和 House1 不一样**。

1. **身份验证先行**：换 house 先跑 prepare+diagnose 验身份链（aggOffW 数百 W / evt 形态壶样 / corr 合理），否则后面全白跑。顺带抓出表头 8 行安装测试残留杂散（probe 定谳，算术闭环到个位数）。
2. **摸底即天花板预警**：baseline 未调参 val F1 已 0.977——头室 <3pt，搜索重心从「提 F1」转向「EE 收敛+多种子稳」。
3. **粗搜 32/32 全过门槛**（守门员在强纪元失区分度）；top-5 挤在 0.0017 内=纯噪声。
4. **赢家诅咒第二现场**：粗搜 top-1（0.0149）细搜 3 fresh seeds **0.0302 垫底**，比不调参锚还差；反而是粗搜 rank-3 的 L1 配置领跑——两纪元同一条教训：**粗搜排名不可选型，只筛方向**。
5. **细搜两批次收敛**：批次 1 七配置（2×2 窗口因子+容量+L1+协议锚），批次 2 加种子到 n=5+两个杂交变体；S 层面 top-3 并列 → 分量显著性定谳（F1 6.2×SEM+recall 五种子零方差=决定性 vs EE 2.2×SEM=边缘）。
6. **Test 终局（seed 9000）**：S 0.0252 / F1 0.9398 / EE **−0.02% 死零**——四线全过。val 重/test 轻的漂移签名预判 EE 偏正**没有兑现**（代价在 recall 不在能量）：EE 方向只能实测，不能推理。

第三纪元的冠军 f5_t9（w192/1 层/bs128）与 H1 的 F4（w96/2 层/bs64）**零参数重叠**——「上一个纪元练出的最优」只是下一个纪元的搜索空间建议，不是答案。

---

## 4. 踩坑清单（工具与纪律，按疼的程度排序）

1. **pandas `fillna(value, limit=N)`**：limit 是全轴总限额（method 未指定时），不是"每段最多 N 格"——240 万缺口格只补了 49 格。
2. **pandas `DataFrame.sum(axis=1)`**：默认跳过 NaN，整行缺失会被静默写成 **0**——多表合并必须 `min_count=1`。
3. **多表时间戳精确 join**：真实数据各表采样相位差秒级，必须先统一网格再对齐。
4. **标签缺口补 0 ≠ 中性**：设备掉线是"传输丢包"不是"电器关机"，补 0 会凭空造出"烧到一半断电"的假模式；ffill 前值才对。
5. **seed 陷阱**：yaml 里 seed 是固定值，同 yaml 跑五次结果全同——必须逐次 `--seed` 覆盖。
6. **PowerShell 裸词列表**：`foreach ($d in a,b,c)` 解析报错——要加引号 `'a','b','c'`。
7. **单文件多处编辑**：并行发多个 edit 会互相覆盖，只活一个——串行或单脚本原子写入。
8. **eval_test 类开关要审默认值**：`--test` 是"强制开"而不是"总开关"，缺省值才是日常行为——纪律要焊在缺省值上。
9. **赢家诅咒（两纪元复现）**：粗搜 top-1 的单 seed 分数是抽签抽出来的——细搜多 fresh seeds 复核两次都打回原形（一次垫底一次大幅回落）。粗搜只用来筛方向，不用来选冠军。
10. **跨纪元引用架构结论**：H1 的最优（L2/w96/bs64）在 H2 全部不是最优（L1/w192/bs128，零重叠）——换数据纪元=搜索空间从旧冠军邻域出发，但每个维度都要重新验证。
11. **`~/.local` 不在持久化范围**：pip --user 装的包跨回合就没了——依赖装完即用，别指望明天还在。
